#include <stdio.h>
#include "main.h"

/**
 * int _abs - function that computes the absolute value
 * of an integer
 *
 * Return: Always 0 (success)
 */

int _abs(int)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
